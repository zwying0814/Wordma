{
  "name": "admin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build --mode=production --outDir ../../backend/server/dist",
    "preview": "vite preview"
  }
}
